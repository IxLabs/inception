* run "make-kernel" inside a kernel source tree
* edit build-lab.sh change KERNEL_DIR to point to <kernel_source>/build
* run "run.sh" with root permissions 
* in each VM console run "./prepare.sh" to configure the nvgre device

